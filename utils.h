#ifndef UTILS_H
#define UTILS_H

float weighted_average(ELEM * current);
float arithmetic_average(ELEM * current);
float institute_average(ELEM * current);
void print_database(ELEM ** phead);

#endif
